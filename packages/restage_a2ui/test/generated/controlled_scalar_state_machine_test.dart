// GENERATED CODE - DO NOT MODIFY BY HAND
// Generated by restage_codegen.
// ignore_for_file: unused_element

import 'dart:async';
import 'controlled_scalar_fixture.dart' as p0;
import 'package:flutter/material.dart';
import 'package:a2ui_core/a2ui_core.dart'
    show CreateSurfaceMessage, UpdateComponentsMessage, UpdateDataModelMessage;
import 'package:flutter_test/flutter_test.dart';
import 'package:genui/genui.dart';
import 'package:json_schema_builder/json_schema_builder.dart';

/// Items for the generated custom-only catalog.
/// A composed Catalog must use a new application-owned catalog ID;
/// do not reuse the generated catalog ID for a different
/// item or schema set.
List<CatalogItem> buildRestageCatalogItems() {
  return <CatalogItem>[
    CatalogItem(
      name: 'ControlledInt',
      dataSchema: S.object(
        properties: {
          'value': S.combined(oneOf: [
            S.combined(anyOf: [S.integer(), S.nil()]),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ])
        },
        required: <String>['value'],
      ),
      widgetBuilder: (itemContext) {
        final data = itemContext.data as Map<String, Object?>;
        final restageA2uiSelfPathValue = '${itemContext.id}.value';
        return _RestageA2uiControlledValue(
          dataContext: itemContext.dataContext,
          source: data['value'],
          sourcePresent: data.containsKey('value'),
          surfaceId: itemContext.surfaceId,
          catalogId: restageA2uiCatalogId,
          componentId: itemContext.id,
          field: 'value',
          selfPath: restageA2uiSelfPathValue,
          reportError: itemContext.reportError,
          builder: (context, restageA2uiRawValue, restageA2uiPresentValue,
              restageA2uiKindValue, restageA2uiWriteValue) {
            final value =
                _restageA2uiNumber(restageA2uiRawValue, restageA2uiKindValue);
            return p0.ControlledIntFixture(
              value: restageA2uiRawValue == null
                  ? null
                  : (value?.toInt() ?? (value ?? 0).toInt()),
              onChanged: restageA2uiWriteValue,
            );
          },
        );
      },
    ),
    CatalogItem(
      name: 'ControlledIntPair',
      dataSchema: S.object(
        properties: {
          'first': S.combined(oneOf: [
            S.combined(anyOf: [S.integer(), S.nil()]),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'second': S.combined(oneOf: [
            S.combined(anyOf: [S.integer(), S.nil()]),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ])
        },
        required: <String>['first', 'second'],
      ),
      widgetBuilder: (itemContext) {
        final data = itemContext.data as Map<String, Object?>;
        final restageA2uiSelfPathFirst = '${itemContext.id}.first';
        final restageA2uiSelfPathSecond = '${itemContext.id}.second';
        return _RestageA2uiControlledValue(
          dataContext: itemContext.dataContext,
          source: data['first'],
          sourcePresent: data.containsKey('first'),
          surfaceId: itemContext.surfaceId,
          catalogId: restageA2uiCatalogId,
          componentId: itemContext.id,
          field: 'first',
          selfPath: restageA2uiSelfPathFirst,
          reportError: itemContext.reportError,
          builder: (context, restageA2uiRawFirst, restageA2uiPresentFirst,
              restageA2uiKindFirst, restageA2uiWriteFirst) {
            final first =
                _restageA2uiNumber(restageA2uiRawFirst, restageA2uiKindFirst);
            return _RestageA2uiControlledValue(
              dataContext: itemContext.dataContext,
              source: data['second'],
              sourcePresent: data.containsKey('second'),
              surfaceId: itemContext.surfaceId,
              catalogId: restageA2uiCatalogId,
              componentId: itemContext.id,
              field: 'second',
              selfPath: restageA2uiSelfPathSecond,
              reportError: itemContext.reportError,
              builder: (context, restageA2uiRawSecond, restageA2uiPresentSecond,
                  restageA2uiKindSecond, restageA2uiWriteSecond) {
                final second = _restageA2uiNumber(
                    restageA2uiRawSecond, restageA2uiKindSecond);
                return p0.ControlledIntPairFixture(
                  first: restageA2uiRawFirst == null
                      ? null
                      : (first?.toInt() ?? (first ?? 0).toInt()),
                  second: restageA2uiRawSecond == null
                      ? null
                      : (second?.toInt() ?? (second ?? 0).toInt()),
                  onFirst: restageA2uiWriteFirst,
                  onSecond: restageA2uiWriteSecond,
                );
              },
            );
          },
        );
      },
    ),
    CatalogItem(
      name: 'ControlledLeafFamilies',
      dataSchema: S.object(
        properties: {
          'enabled': S.combined(oneOf: [
            S.boolean(),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'label': S.combined(oneOf: [
            S.string(),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'choice': S.combined(oneOf: [
            S.string(enumValues: <Object?>['alpha', 'beta']),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'strings': S.combined(oneOf: [
            S.list(items: S.string()),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'integers': S.combined(oneOf: [
            S.list(items: S.integer()),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'doubles': S.combined(oneOf: [
            S.list(items: S.number()),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'numbers': S.combined(oneOf: [
            S.list(items: S.number()),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'booleans': S.combined(oneOf: [
            S.list(items: S.boolean()),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'maybeNumbers': S.combined(oneOf: [
            S.combined(anyOf: [
              S.list(items: S.combined(anyOf: [S.number(), S.nil()])),
              S.nil()
            ]),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'maybeIntegers': S.combined(oneOf: [
            S.combined(anyOf: [S.list(items: S.integer()), S.nil()]),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ]),
          'fallbackIntegers': S.combined(oneOf: [
            S.combined(anyOf: [S.list(items: S.integer()), S.nil()]),
            S.object(
                properties: {'path': S.string()}, required: <String>['path']),
            S.object(properties: {
              'call': S.string(),
              'args': S.object(additionalProperties: true)
            }, required: <String>[
              'call'
            ])
          ])
        },
        required: <String>[
          'enabled',
          'label',
          'choice',
          'strings',
          'integers',
          'doubles',
          'numbers',
          'booleans',
          'maybeNumbers'
        ],
      ),
      widgetBuilder: (itemContext) {
        final data = itemContext.data as Map<String, Object?>;
        final restageA2uiSelfPathEnabled = '${itemContext.id}.enabled';
        final restageA2uiSelfPathLabel = '${itemContext.id}.label';
        final restageA2uiSelfPathChoice = '${itemContext.id}.choice';
        final restageA2uiSelfPathStrings = '${itemContext.id}.strings';
        final restageA2uiSelfPathIntegers = '${itemContext.id}.integers';
        final restageA2uiSelfPathDoubles = '${itemContext.id}.doubles';
        final restageA2uiSelfPathNumbers = '${itemContext.id}.numbers';
        final restageA2uiSelfPathBooleans = '${itemContext.id}.booleans';
        final restageA2uiSelfPathMaybeIntegers =
            '${itemContext.id}.maybeIntegers';
        final restageA2uiSelfPathMaybeNumbers =
            '${itemContext.id}.maybeNumbers';
        final restageA2uiSelfPathFallbackIntegers =
            '${itemContext.id}.fallbackIntegers';
        return _RestageA2uiControlledValue(
          dataContext: itemContext.dataContext,
          source: data['enabled'],
          sourcePresent: data.containsKey('enabled'),
          surfaceId: itemContext.surfaceId,
          catalogId: restageA2uiCatalogId,
          componentId: itemContext.id,
          field: 'enabled',
          selfPath: restageA2uiSelfPathEnabled,
          reportError: itemContext.reportError,
          builder: (context, restageA2uiRawEnabled, restageA2uiPresentEnabled,
              restageA2uiKindEnabled, restageA2uiWriteEnabled) {
            final enabled =
                _restageA2uiBool(restageA2uiRawEnabled, restageA2uiKindEnabled);
            return _RestageA2uiControlledValue(
              dataContext: itemContext.dataContext,
              source: data['label'],
              sourcePresent: data.containsKey('label'),
              surfaceId: itemContext.surfaceId,
              catalogId: restageA2uiCatalogId,
              componentId: itemContext.id,
              field: 'label',
              selfPath: restageA2uiSelfPathLabel,
              reportError: itemContext.reportError,
              builder: (context, restageA2uiRawLabel, restageA2uiPresentLabel,
                  restageA2uiKindLabel, restageA2uiWriteLabel) {
                final label = _restageA2uiString(restageA2uiRawLabel);
                return _RestageA2uiControlledValue(
                  dataContext: itemContext.dataContext,
                  source: data['choice'],
                  sourcePresent: data.containsKey('choice'),
                  surfaceId: itemContext.surfaceId,
                  catalogId: restageA2uiCatalogId,
                  componentId: itemContext.id,
                  field: 'choice',
                  selfPath: restageA2uiSelfPathChoice,
                  reportError: itemContext.reportError,
                  builder: (context,
                      restageA2uiRawChoice,
                      restageA2uiPresentChoice,
                      restageA2uiKindChoice,
                      restageA2uiWriteChoice) {
                    final choice = _restageA2uiEnumName(restageA2uiRawChoice);
                    return _RestageA2uiControlledValue(
                      dataContext: itemContext.dataContext,
                      source: data['strings'],
                      sourcePresent: data.containsKey('strings'),
                      surfaceId: itemContext.surfaceId,
                      catalogId: restageA2uiCatalogId,
                      componentId: itemContext.id,
                      field: 'strings',
                      selfPath: restageA2uiSelfPathStrings,
                      reportError: itemContext.reportError,
                      builder: (context,
                          restageA2uiRawStrings,
                          restageA2uiPresentStrings,
                          restageA2uiKindStrings,
                          restageA2uiWriteStrings) {
                        final strings = restageA2uiRawStrings;
                        return _RestageA2uiControlledValue(
                          dataContext: itemContext.dataContext,
                          source: data['integers'],
                          sourcePresent: data.containsKey('integers'),
                          surfaceId: itemContext.surfaceId,
                          catalogId: restageA2uiCatalogId,
                          componentId: itemContext.id,
                          field: 'integers',
                          selfPath: restageA2uiSelfPathIntegers,
                          reportError: itemContext.reportError,
                          builder: (context,
                              restageA2uiRawIntegers,
                              restageA2uiPresentIntegers,
                              restageA2uiKindIntegers,
                              restageA2uiWriteIntegers) {
                            final integers = restageA2uiRawIntegers;
                            return _RestageA2uiControlledValue(
                              dataContext: itemContext.dataContext,
                              source: data['doubles'],
                              sourcePresent: data.containsKey('doubles'),
                              surfaceId: itemContext.surfaceId,
                              catalogId: restageA2uiCatalogId,
                              componentId: itemContext.id,
                              field: 'doubles',
                              selfPath: restageA2uiSelfPathDoubles,
                              reportError: itemContext.reportError,
                              builder: (context,
                                  restageA2uiRawDoubles,
                                  restageA2uiPresentDoubles,
                                  restageA2uiKindDoubles,
                                  restageA2uiWriteDoubles) {
                                final doubles = restageA2uiRawDoubles;
                                return _RestageA2uiControlledValue(
                                  dataContext: itemContext.dataContext,
                                  source: data['numbers'],
                                  sourcePresent: data.containsKey('numbers'),
                                  surfaceId: itemContext.surfaceId,
                                  catalogId: restageA2uiCatalogId,
                                  componentId: itemContext.id,
                                  field: 'numbers',
                                  selfPath: restageA2uiSelfPathNumbers,
                                  reportError: itemContext.reportError,
                                  builder: (context,
                                      restageA2uiRawNumbers,
                                      restageA2uiPresentNumbers,
                                      restageA2uiKindNumbers,
                                      restageA2uiWriteNumbers) {
                                    final numbers = restageA2uiRawNumbers;
                                    return _RestageA2uiControlledValue(
                                      dataContext: itemContext.dataContext,
                                      source: data['booleans'],
                                      sourcePresent:
                                          data.containsKey('booleans'),
                                      surfaceId: itemContext.surfaceId,
                                      catalogId: restageA2uiCatalogId,
                                      componentId: itemContext.id,
                                      field: 'booleans',
                                      selfPath: restageA2uiSelfPathBooleans,
                                      reportError: itemContext.reportError,
                                      builder: (context,
                                          restageA2uiRawBooleans,
                                          restageA2uiPresentBooleans,
                                          restageA2uiKindBooleans,
                                          restageA2uiWriteBooleans) {
                                        final booleans = restageA2uiRawBooleans;
                                        return _RestageA2uiControlledValue(
                                          dataContext: itemContext.dataContext,
                                          source: data['maybeNumbers'],
                                          sourcePresent:
                                              data.containsKey('maybeNumbers'),
                                          surfaceId: itemContext.surfaceId,
                                          catalogId: restageA2uiCatalogId,
                                          componentId: itemContext.id,
                                          field: 'maybeNumbers',
                                          selfPath:
                                              restageA2uiSelfPathMaybeNumbers,
                                          reportError: itemContext.reportError,
                                          builder: (context,
                                              restageA2uiRawMaybeNumbers,
                                              restageA2uiPresentMaybeNumbers,
                                              restageA2uiKindMaybeNumbers,
                                              restageA2uiWriteMaybeNumbers) {
                                            final maybeNumbers =
                                                restageA2uiRawMaybeNumbers;
                                            return _RestageA2uiControlledValue(
                                              dataContext:
                                                  itemContext.dataContext,
                                              source: data['maybeIntegers'],
                                              sourcePresent: data
                                                  .containsKey('maybeIntegers'),
                                              surfaceId: itemContext.surfaceId,
                                              catalogId: restageA2uiCatalogId,
                                              componentId: itemContext.id,
                                              field: 'maybeIntegers',
                                              selfPath:
                                                  restageA2uiSelfPathMaybeIntegers,
                                              reportError:
                                                  itemContext.reportError,
                                              builder: (context,
                                                  restageA2uiRawMaybeIntegers,
                                                  restageA2uiPresentMaybeIntegers,
                                                  restageA2uiKindMaybeIntegers,
                                                  restageA2uiWriteMaybeIntegers) {
                                                final maybeIntegers =
                                                    restageA2uiRawMaybeIntegers;
                                                return _RestageA2uiControlledValue(
                                                  dataContext:
                                                      itemContext.dataContext,
                                                  source:
                                                      data['fallbackIntegers'],
                                                  sourcePresent:
                                                      data.containsKey(
                                                          'fallbackIntegers'),
                                                  surfaceId:
                                                      itemContext.surfaceId,
                                                  catalogId:
                                                      restageA2uiCatalogId,
                                                  componentId: itemContext.id,
                                                  field: 'fallbackIntegers',
                                                  selfPath:
                                                      restageA2uiSelfPathFallbackIntegers,
                                                  reportError:
                                                      itemContext.reportError,
                                                  builder: (context,
                                                      restageA2uiRawFallbackIntegers,
                                                      restageA2uiPresentFallbackIntegers,
                                                      restageA2uiKindFallbackIntegers,
                                                      restageA2uiWriteFallbackIntegers) {
                                                    final fallbackIntegers =
                                                        restageA2uiRawFallbackIntegers;
                                                    return p0
                                                        .ControlledLeafFamiliesFixture(
                                                      enabled: enabled ?? false,
                                                      label: label ?? '',
                                                      choice: p0.ControlledChoice
                                                                  .values
                                                                  .asNameMap()[
                                                              choice] ??
                                                          p0.ControlledChoice
                                                              .values.first,
                                                      strings: ((strings is List
                                                                  ? strings.cast<
                                                                      Object?>()
                                                                  : null) ??
                                                              const <Object?>[])
                                                          .whereType<String>()
                                                          .toList(
                                                              growable: false),
                                                      integers: ((integers
                                                                      is List
                                                                  ? integers.cast<
                                                                      Object?>()
                                                                  : null) ??
                                                              const <Object?>[])
                                                          .map((value) => value
                                                                  is num
                                                              ? value.toInt()
                                                              : null)
                                                          .whereType<int>()
                                                          .toList(
                                                              growable: false),
                                                      doubles: ((doubles is List
                                                                  ? doubles.cast<
                                                                      Object?>()
                                                                  : null) ??
                                                              const <Object?>[])
                                                          .map((value) => value
                                                                  is num
                                                              ? value.toDouble()
                                                              : null)
                                                          .whereType<double>()
                                                          .toList(
                                                              growable: false),
                                                      numbers: ((numbers is List
                                                                  ? numbers.cast<
                                                                      Object?>()
                                                                  : null) ??
                                                              const <Object?>[])
                                                          .map((value) =>
                                                              value is num
                                                                  ? value
                                                                  : null)
                                                          .whereType<num>()
                                                          .toList(
                                                              growable: false),
                                                      booleans: ((booleans
                                                                      is List
                                                                  ? booleans.cast<
                                                                      Object?>()
                                                                  : null) ??
                                                              const <Object?>[])
                                                          .whereType<bool>()
                                                          .toList(
                                                              growable: false),
                                                      maybeNumbers: (restageA2uiRawMaybeNumbers ==
                                                                  null
                                                              ? null
                                                              : (maybeNumbers
                                                                      is List
                                                                  ? maybeNumbers
                                                                      .cast<
                                                                          Object?>()
                                                                  : null))
                                                          ?.map((value) =>
                                                              value is num
                                                                  ? value
                                                                  : null)
                                                          .toList(
                                                              growable: false),
                                                      maybeIntegers: (restageA2uiPresentMaybeIntegers
                                                              ? (restageA2uiRawMaybeIntegers ==
                                                                      null
                                                                  ? null
                                                                  : (maybeIntegers
                                                                          is List
                                                                      ? maybeIntegers
                                                                          .cast<
                                                                              Object?>()
                                                                      : null))
                                                              : null)
                                                          ?.map((value) => value
                                                                  is num
                                                              ? value.toInt()
                                                              : null)
                                                          .whereType<int>()
                                                          .toList(
                                                              growable: false),
                                                      fallbackIntegers: (restageA2uiPresentFallbackIntegers
                                                              ? (restageA2uiRawFallbackIntegers ==
                                                                      null
                                                                  ? null
                                                                  : ((fallbackIntegers
                                                                              is List
                                                                          ? fallbackIntegers.cast<
                                                                              Object?>()
                                                                          : null) ??
                                                                      const <int>[
                                                                        7,
                                                                        8
                                                                      ]))
                                                              : const <int>[
                                                                  7,
                                                                  8
                                                                ])
                                                          ?.map((value) => value
                                                                  is num
                                                              ? value.toInt()
                                                              : null)
                                                          .whereType<int>()
                                                          .toList(
                                                              growable: false),
                                                      onEnabled:
                                                          restageA2uiWriteEnabled,
                                                      onLabel:
                                                          restageA2uiWriteLabel,
                                                      onChoice: (restageA2uiNext) =>
                                                          restageA2uiWriteChoice(
                                                              restageA2uiNext
                                                                  .name),
                                                      onStrings:
                                                          restageA2uiWriteStrings,
                                                      onIntegers:
                                                          restageA2uiWriteIntegers,
                                                      onDoubles:
                                                          restageA2uiWriteDoubles,
                                                      onNumbers:
                                                          restageA2uiWriteNumbers,
                                                      onBooleans:
                                                          restageA2uiWriteBooleans,
                                                      onMaybeIntegers:
                                                          restageA2uiWriteMaybeIntegers,
                                                      onMaybeNumbers:
                                                          restageA2uiWriteMaybeNumbers,
                                                      onFallbackIntegers:
                                                          restageA2uiWriteFallbackIntegers,
                                                    );
                                                  },
                                                );
                                              },
                                            );
                                          },
                                        );
                                      },
                                    );
                                  },
                                );
                              },
                            );
                          },
                        );
                      },
                    );
                  },
                );
              },
            );
          },
        );
      },
    ),
    CatalogItem(
      name: 'ControlledScalarHost',
      dataSchema: S.object(
        properties: {'child': S.string()},
        required: <String>['child'],
      ),
      widgetBuilder: (itemContext) {
        final data = itemContext.data as Map<String, Object?>;
        return p0.ControlledScalarHostFixture(
          child: _restageA2uiRequireChild(
              itemContext, data['child'], 'ControlledScalarHost.child'),
        );
      },
    ),
  ];
}

/// Content address for exactly the generated custom-only catalog.
/// Default A2A supportedCatalogIds use requires server registration
/// of this exact predefined catalog contract.
/// GenUI 0.10.1 inline catalogs are serialization-only here;
/// no end-to-end inline server interoperability is claimed.
const String restageA2uiCatalogId =
    'restage:catalog/sha256/dec8541a3fcbc4d1cd86f9f21ce38fa9951419d4197c7d7f98ef5f6693176d91';

const List<String> _restageA2uiSystemPromptFragments = <String>[
  'For every A2UI createSurface message, set catalogId to "restage:catalog/sha256/dec8541a3fcbc4d1cd86f9f21ce38fa9951419d4197c7d7f98ef5f6693176d91".',
];

/// Builds the generated custom-only GenUI catalog identified by
/// [restageA2uiCatalogId].
///
/// To compose a different catalog, use
/// [buildRestageCatalogItems] and assign the new Catalog an
/// application-owned catalog ID.
Catalog buildRestageCatalog() => Catalog(
      buildRestageCatalogItems(),
      catalogId: restageA2uiCatalogId,
      systemPromptFragments: _restageA2uiSystemPromptFragments,
    );

Widget? _restageA2uiBuildChild(
    CatalogItemContext itemContext, Object? childId) {
  if (childId is! String || childId.isEmpty) return null;
  return itemContext.buildChild(childId);
}

Never _restageA2uiRequiredChildError(Object? childId, String propertyContext) {
  final String reason;
  if (childId == null) {
    reason = 'the value was null or missing';
  } else if (childId is! String) {
    reason = 'the value had runtime type ${childId.runtimeType}, '
        'but a String component id is required';
  } else if (childId.isEmpty) {
    reason = 'the value was the empty string';
  } else {
    reason = 'component id "$childId" is not registered';
  }
  throw StateError(
    'Required A2UI child "$propertyContext" could not resolve: '
    '$reason. Provide a non-empty String id for a component '
    'registered on this surface.',
  );
}

Never _restageA2uiRequiredChildBuildError(String childId,
    String propertyContext, Object error, StackTrace stackTrace) {
  Error.throwWithStackTrace(
      StateError(
        'Required A2UI child "$propertyContext" with component id '
        '"$childId" failed to build: $error',
      ),
      stackTrace);
}

Widget _restageA2uiRequireChild(
    CatalogItemContext itemContext, Object? childId, String propertyContext) {
  if (childId is! String || childId.isEmpty) {
    _restageA2uiRequiredChildError(childId, propertyContext);
  }
  if (itemContext.getComponent(childId) == null) {
    _restageA2uiRequiredChildError(childId, propertyContext);
  }
  late final Widget child;
  try {
    child = itemContext.buildChild(childId);
  } catch (error, stackTrace) {
    _restageA2uiRequiredChildBuildError(
        childId, propertyContext, error, stackTrace);
  }
  if (child is FallbackWidget && child.error != null) {
    _restageA2uiRequiredChildBuildError(childId, propertyContext, child.error!,
        child.stackTrace ?? StackTrace.current);
  }
  return child;
}

List<Widget> _restageA2uiBuildChildren(
    CatalogItemContext itemContext, Object? childIds) {
  if (childIds is! List<Object?>) return const <Widget>[];
  return <Widget>[
    for (final childId in childIds)
      if (_restageA2uiBuildChild(itemContext, childId) != null)
        _restageA2uiBuildChild(itemContext, childId)!,
  ];
}

Color? _restageA2uiColor(String? value) {
  if (value == null || value.isEmpty) return null;
  final normalized = value.startsWith('#')
      ? value.substring(1)
      : value.startsWith(
          '0x',
        )
          ? value.substring(2)
          : value;
  final parsed = int.tryParse(normalized, radix: 16);
  if (parsed == null) return null;
  if (normalized.length <= 6) return Color(0xFF000000 | parsed);
  return Color(parsed);
}

FontWeight _restageA2uiFontWeight(
  num? value,
  FontWeight fallback,
) {
  if (value == null) return fallback;
  final index = value.toInt();
  if (index < 0 || index >= FontWeight.values.length) {
    return fallback;
  }
  return FontWeight.values[index];
}

enum _RestageA2uiSourceKind { literal, path, call, localOverride }

typedef _RestageA2uiControlledBuilder = Widget Function(
  BuildContext context,
  Object? rawValue,
  bool sourcePresent,
  _RestageA2uiSourceKind sourceKind,
  ValueChanged<Object?> write,
);

final class _RestageA2uiControlledValue extends StatefulWidget {
  const _RestageA2uiControlledValue({
    required this.dataContext,
    required this.source,
    required this.sourcePresent,
    required this.surfaceId,
    required this.catalogId,
    required this.componentId,
    required this.field,
    required this.selfPath,
    required this.reportError,
    required this.builder,
  });

  final DataContext dataContext;
  final Object? source;
  final bool sourcePresent;
  final String surfaceId;
  final String? catalogId;
  final String componentId;
  final String field;
  final String selfPath;
  final void Function(Object error, StackTrace? stack) reportError;
  final _RestageA2uiControlledBuilder builder;

  @override
  State<_RestageA2uiControlledValue> createState() =>
      _RestageA2uiControlledValueState();
}

final class _RestageA2uiControlledValueState
    extends State<_RestageA2uiControlledValue> {
  StreamSubscription<Object?>? _subscription;
  void Function(Object error, StackTrace? stack)? _subscriptionReportError;
  var _epoch = 0;
  late _RestageA2uiSourceDescriptor _descriptor;
  late _RestageA2uiSemanticIdentity _semanticIdentity;
  Object? _sourceValue;
  var _sourcePresent = false;
  var _hasOverride = false;
  Object? _overrideValue;

  @override
  void initState() {
    super.initState();
    _adoptBinding(
      _RestageA2uiSourceDescriptor.from(widget.source),
      _RestageA2uiSemanticIdentity.from(widget),
    );
    _subscribe();
  }

  @override
  void didUpdateWidget(_RestageA2uiControlledValue oldWidget) {
    super.didUpdateWidget(oldWidget);
    final nextDescriptor = _RestageA2uiSourceDescriptor.from(widget.source);
    final nextSemantic = _RestageA2uiSemanticIdentity.from(widget);
    final bindingChanged = !_descriptor.sameBinding(nextDescriptor) ||
        _semanticIdentity != nextSemantic ||
        oldWidget.componentId != widget.componentId ||
        oldWidget.field != widget.field ||
        oldWidget.selfPath != widget.selfPath;
    if (bindingChanged) {
      _invalidateSubscription();
      _adoptBinding(nextDescriptor, nextSemantic);
      _subscribe();
      return;
    }

    final literalPayloadChanged =
        nextDescriptor.kind == _RestageA2uiSourceKind.literal &&
            (!_restageA2uiLiteralEqual(oldWidget.source, widget.source) ||
                oldWidget.sourcePresent != widget.sourcePresent);
    if (literalPayloadChanged && !_hasOverride) {
      _invalidateSubscription();
      _sourceValue = widget.source;
      _sourcePresent = widget.sourcePresent;
      _subscribe();
    }
  }

  void _adoptBinding(
    _RestageA2uiSourceDescriptor descriptor,
    _RestageA2uiSemanticIdentity semanticIdentity,
  ) {
    _descriptor = descriptor;
    _semanticIdentity = semanticIdentity;
    _sourceValue = switch (descriptor.kind) {
      _RestageA2uiSourceKind.literal => widget.source,
      _RestageA2uiSourceKind.path =>
        widget.dataContext.getValue<Object?>(DataPath(descriptor.path!)),
      _RestageA2uiSourceKind.call ||
      _RestageA2uiSourceKind.localOverride =>
        null,
    };
    _sourcePresent = widget.sourcePresent;
    _hasOverride = false;
    _overrideValue = null;
  }

  void _subscribe() {
    final subscribedEpoch = _epoch;
    final literalPresence = widget.sourcePresent;
    final reportError = widget.reportError;
    _subscriptionReportError = reportError;
    _subscription = widget.dataContext.resolve(widget.source).listen(
      (value) {
        if (!mounted || subscribedEpoch != _epoch || _hasOverride) return;
        setState(() {
          _sourceValue = value;
          _sourcePresent = _descriptor.kind == _RestageA2uiSourceKind.literal
              ? literalPresence
              : true;
        });
      },
      onError: (Object error, StackTrace stack) {
        if (!mounted || subscribedEpoch != _epoch) return;
        reportError(error, stack);
      },
    );
  }

  void _invalidateSubscription() {
    final previous = _subscription;
    final reportError = _subscriptionReportError ?? widget.reportError;
    _subscription = null;
    _subscriptionReportError = null;
    _epoch += 1;
    if (previous == null) return;
    Future<void> cancellation;
    try {
      cancellation = previous.cancel();
    } catch (error, stack) {
      reportError(error, stack);
      return;
    }
    unawaited(
      cancellation.then<void>(
        (_) {},
        onError: (Object error, StackTrace stack) {
          reportError(error, stack);
        },
      ),
    );
  }

  void _write(Object? next) {
    if (!mounted) return;
    if (_descriptor.kind == _RestageA2uiSourceKind.path) {
      widget.dataContext.update(DataPath(_descriptor.path!), next);
      return;
    }

    _invalidateSubscription();
    _hasOverride = true;
    _overrideValue = next;
    widget.dataContext.update(DataPath(widget.selfPath), next);
    if (mounted) setState(() {});
  }

  @override
  Widget build(BuildContext context) {
    final overridden = _hasOverride;
    return widget.builder(
      context,
      overridden ? _overrideValue : _sourceValue,
      overridden ? true : _sourcePresent,
      overridden ? _RestageA2uiSourceKind.localOverride : _descriptor.kind,
      _write,
    );
  }

  @override
  void dispose() {
    _invalidateSubscription();
    super.dispose();
  }
}

final class _RestageA2uiSourceDescriptor {
  const _RestageA2uiSourceDescriptor._(
    this.kind, {
    this.path,
    this.callName,
    this.callArgs,
  });

  factory _RestageA2uiSourceDescriptor.from(Object? source) {
    if (source is Map && source.containsKey('path')) {
      final path = source['path'];
      if (path is! String) {
        throw ArgumentError.value(
          path,
          'source.path',
          'A controlled A2UI path must be a String.',
        );
      }
      return _RestageA2uiSourceDescriptor._(
        _RestageA2uiSourceKind.path,
        path: path,
      );
    }
    if (source is Map && source.containsKey('call')) {
      final callName = source['call'];
      if (callName != null && callName is! String) {
        throw ArgumentError.value(
          callName,
          'source.call',
          'A controlled A2UI call name must be a String or null.',
        );
      }
      final args = source['args'];
      return _RestageA2uiSourceDescriptor._(
        _RestageA2uiSourceKind.call,
        callName: callName as String?,
        callArgs: args is Map ? args : const <String, Object?>{},
      );
    }
    return const _RestageA2uiSourceDescriptor._(
      _RestageA2uiSourceKind.literal,
    );
  }

  final _RestageA2uiSourceKind kind;
  final String? path;
  final String? callName;
  final Map<Object?, Object?>? callArgs;

  bool sameBinding(_RestageA2uiSourceDescriptor other) {
    if (kind != other.kind) return false;
    return switch (kind) {
      _RestageA2uiSourceKind.literal => true,
      _RestageA2uiSourceKind.path => path == other.path,
      _RestageA2uiSourceKind.call => callName == other.callName &&
          _restageA2uiCallIdentityEqual(callArgs, other.callArgs),
      _RestageA2uiSourceKind.localOverride => false,
    };
  }
}

final class _RestageA2uiSemanticIdentity {
  const _RestageA2uiSemanticIdentity(
    this.surfaceId,
    this.catalogId,
    this.dataModel,
    this.contextPath,
  );

  factory _RestageA2uiSemanticIdentity.from(
    _RestageA2uiControlledValue widget,
  ) =>
      _RestageA2uiSemanticIdentity(
        widget.surfaceId,
        widget.catalogId,
        widget.dataContext.dataModel,
        widget.dataContext.path,
      );

  final String surfaceId;
  final String? catalogId;
  final Object dataModel;
  final DataPath contextPath;

  @override
  bool operator ==(Object other) =>
      other is _RestageA2uiSemanticIdentity &&
      surfaceId == other.surfaceId &&
      catalogId == other.catalogId &&
      identical(dataModel, other.dataModel) &&
      contextPath == other.contextPath;

  @override
  int get hashCode => Object.hash(
        surfaceId,
        catalogId,
        identityHashCode(dataModel),
        contextPath,
      );
}

bool _restageA2uiCallIdentityEqual(Object? left, Object? right) {
  if (identical(left, right)) return true;
  if (left is num && right is num) return left == right;
  if (left is List && right is List) {
    if (left.length != right.length) return false;
    for (var index = 0; index < left.length; index += 1) {
      if (!_restageA2uiCallIdentityEqual(left[index], right[index])) {
        return false;
      }
    }
    return true;
  }
  if (left is Map && right is Map) {
    if (left.length != right.length) return false;
    for (final entry in left.entries) {
      if (!right.containsKey(entry.key) ||
          !_restageA2uiCallIdentityEqual(entry.value, right[entry.key])) {
        return false;
      }
    }
    return true;
  }
  return left == right;
}

bool _restageA2uiLiteralEqual(Object? left, Object? right) {
  if (identical(left, right)) return true;
  if (left == null || right == null) return false;
  if (left is num && right is num) {
    return left.runtimeType == right.runtimeType && left == right;
  }
  if (left is List && right is List) {
    if (left.length != right.length) return false;
    for (var index = 0; index < left.length; index += 1) {
      if (!_restageA2uiLiteralEqual(left[index], right[index])) return false;
    }
    return true;
  }
  if (left is Map && right is Map) {
    if (left.length != right.length) return false;
    for (final entry in left.entries) {
      if (!right.containsKey(entry.key) ||
          !_restageA2uiLiteralEqual(entry.value, right[entry.key])) {
        return false;
      }
    }
    return true;
  }
  return left.runtimeType == right.runtimeType && left == right;
}

num? _restageA2uiNumber(
  Object? rawValue,
  _RestageA2uiSourceKind sourceKind,
) {
  if (rawValue is num) return rawValue;
  if ((sourceKind == _RestageA2uiSourceKind.path ||
          sourceKind == _RestageA2uiSourceKind.call) &&
      rawValue is String) {
    return num.tryParse(rawValue);
  }
  return null;
}

bool? _restageA2uiBool(
  Object? rawValue,
  _RestageA2uiSourceKind sourceKind,
) {
  if (rawValue is bool) return rawValue;
  if (sourceKind == _RestageA2uiSourceKind.path) {
    if (rawValue is String) {
      final normalized = rawValue.toLowerCase();
      if (normalized == 'true') return true;
      if (normalized == 'false') return false;
    }
    if (rawValue is num) return rawValue != 0;
  }
  if (sourceKind == _RestageA2uiSourceKind.call) {
    return rawValue != null;
  }
  return null;
}

String? _restageA2uiString(Object? rawValue) => rawValue?.toString();

String? _restageA2uiEnumName(Object? rawValue) =>
    rawValue is Enum ? rawValue.name : rawValue?.toString();

const _alternateExactCatalogId = 'restage:catalog/sha256/d5c7f85cb02621d38e6af177a92e557a14319ac091f8a2de5fd9cf4fad2d0fd9';

final class _ControlledIntFunction implements ClientFunction {
  _ControlledIntFunction(this.controller);

  final StreamController<Object?> controller;
  int executeCount = 0;

  @override
  String get name => 'currentInt';

  @override
  String get description => 'Emits the current integer.';

  @override
  Schema get argumentSchema => S.object(properties: const {});

  @override
  ClientFunctionReturnType get returnType => ClientFunctionReturnType.number;

  @override
  Stream<Object?> execute(JsonMap args, ExecutionContext context) {
    executeCount++;
    return controller.stream;
  }
}

final class _ControlledValueFunction implements ClientFunction {
  _ControlledValueFunction(this.name, this.controller);

  @override
  final String name;
  final StreamController<Object?> controller;

  @override
  String get description => 'Emits a controlled proof value.';

  @override
  Schema get argumentSchema => S.object(properties: const {});

  @override
  ClientFunctionReturnType get returnType => ClientFunctionReturnType.any;

  @override
  Stream<Object?> execute(JsonMap args, ExecutionContext context) =>
      controller.stream;
}

final class _LateAfterCancelClientFunction implements ClientFunction {
  _LateAfterCancelClientFunction(
    this.name,
    this.events, {
    this.cancelFuture,
  });

  @override
  final String name;
  final List<String> events;
  final Future<void>? cancelFuture;
  final List<_LateAfterCancelStream> executions = [];

  @override
  String get description => 'Delivers deterministic events after cancel.';

  @override
  Schema get argumentSchema => S.object(additionalProperties: true);

  @override
  ClientFunctionReturnType get returnType => ClientFunctionReturnType.number;

  @override
  Stream<Object?> execute(JsonMap args, ExecutionContext context) {
    final sequence = executions.length + 1;
    events.add('execute:$name:$sequence');
    final stream = _LateAfterCancelStream(
      onCancel: () => events.add('cancel:$name:$sequence'),
      cancelFuture: cancelFuture,
    );
    executions.add(stream);
    return stream;
  }
}

final class _LateAfterCancelStream extends Stream<Object?> {
  _LateAfterCancelStream({
    required this.onCancel,
    this.cancelFuture,
  });

  final VoidCallback onCancel;
  final Future<void>? cancelFuture;
  void Function(Object?)? _handleData;
  var _isPaused = false;
  var _isCancelled = false;
  var cancelCount = 0;

  void emitLate(Object? value) => _handleData?.call(value);

  @override
  StreamSubscription<Object?> listen(
    void Function(Object?)? onData, {
    Function? onError,
    void Function()? onDone,
    bool? cancelOnError,
  }) {
    _handleData = onData;
    return _LateAfterCancelSubscription(this);
  }
}

final class _LateAfterCancelSubscription
    implements StreamSubscription<Object?> {
  _LateAfterCancelSubscription(this.stream);

  final _LateAfterCancelStream stream;

  @override
  Future<E> asFuture<E>([E? futureValue]) => Completer<E>().future;

  @override
  Future<void> cancel() {
    if (!stream._isCancelled) {
      stream._isCancelled = true;
      stream.cancelCount++;
      stream.onCancel();
    }
    return stream.cancelFuture ?? Future<void>.value();
  }

  @override
  bool get isPaused => stream._isPaused;

  @override
  void onData(void Function(Object?)? handleData) {
    stream._handleData = handleData;
  }

  @override
  void onDone(void Function()? handleDone) {}

  @override
  void onError(Function? handleError) {}

  @override
  void pause([Future<void>? resumeSignal]) {
    stream._isPaused = true;
    resumeSignal?.whenComplete(resume);
  }

  @override
  void resume() {
    stream._isPaused = false;
  }
}

final class _ItemConfiguration {
  const _ItemConfiguration({
    required this.type,
    required this.data,
    required this.id,
    required this.surfaceId,
    required this.dataContext,
  });

  final String type;
  final Map<String, Object?> data;
  final String id;
  final String surfaceId;
  final DataContext dataContext;
}

CatalogItem? _itemNamed(List<CatalogItem> items, String type) {
  for (final item in items) {
    if (item.name == type) return item;
  }
  return null;
}

Widget _buildConfiguredItem(
  BuildContext context,
  Catalog catalog,
  List<CatalogItem> items,
  _ItemConfiguration configuration,
) =>
    catalog.buildWidget(
      CatalogItemContext(
        data: configuration.data,
        id: configuration.id,
        type: configuration.type,
        buildChild: (id, [dataContext]) => const SizedBox.shrink(),
        dispatchEvent: (_) {},
        buildContext: context,
        dataContext: configuration.dataContext,
        getComponent: (_) => null,
        getCatalogItem: (type) => _itemNamed(items, type),
        surfaceId: configuration.surfaceId,
        reportError: (error, stack) => Error.throwWithStackTrace(
          error,
          stack ?? StackTrace.current,
        ),
      ),
    );

Future<void> _pumpConfiguredItem(
  WidgetTester tester,
  ValueNotifier<_ItemConfiguration> configuration,
) async {
  final items = buildRestageCatalogItems();
  final catalog = Catalog(items, catalogId: restageA2uiCatalogId);
  await tester.pumpWidget(
    MaterialApp(
      home: Builder(
        builder: (context) => ValueListenableBuilder<_ItemConfiguration>(
          valueListenable: configuration,
          builder: (context, value, _) =>
              _buildConfiguredItem(context, catalog, items, value),
        ),
      ),
    ),
  );
  await tester.pump();
}

Future<void> _pumpTwoConfiguredItems(
  WidgetTester tester,
  ValueNotifier<List<_ItemConfiguration>> configurations,
) async {
  final items = buildRestageCatalogItems();
  final catalog = Catalog(items, catalogId: restageA2uiCatalogId);
  await tester.pumpWidget(
    MaterialApp(
      home: Builder(
        builder: (context) =>
            ValueListenableBuilder<List<_ItemConfiguration>>(
          valueListenable: configurations,
          builder: (context, values, _) => Column(
            children: [
              for (final value in values)
                _buildConfiguredItem(context, catalog, items, value),
            ],
          ),
        ),
      ),
    ),
  );
  await tester.pump();
}

Map<String, Object?> _controlledFamiliesComponent(Object choice) => {
      'id': 'root',
      'component': 'ControlledLeafFamilies',
      'enabled': true,
      'label': 'surface',
      'choice': choice,
      'strings': const <String>[],
      'integers': const <int>[],
      'doubles': const <double>[],
      'numbers': const <num>[],
      'booleans': const <bool>[],
      'maybeNumbers': const <num?>[],
    };

final class _DirectConfiguration {
  const _DirectConfiguration({
    required this.source,
    required this.sourcePresent,
    required this.dataContext,
    required this.surfaceId,
    required this.catalogId,
    required this.componentId,
    required this.field,
    required this.selfPath,
    this.reportError,
  });

  final Object? source;
  final bool sourcePresent;
  final DataContext dataContext;
  final String surfaceId;
  final String catalogId;
  final String componentId;
  final String field;
  final String selfPath;
  final void Function(Object error, StackTrace? stack)? reportError;
}

_DirectConfiguration _directConfiguration(
  DataContext dataContext, {
  Object? source = 7,
  bool sourcePresent = true,
  String surfaceId = 'surface-a',
  String catalogId = restageA2uiCatalogId,
  String componentId = 'root',
  String field = 'value',
  String? selfPath,
  void Function(Object error, StackTrace? stack)? reportError,
}) =>
    _DirectConfiguration(
      source: source,
      sourcePresent: sourcePresent,
      dataContext: dataContext,
      surfaceId: surfaceId,
      catalogId: catalogId,
      componentId: componentId,
      field: field,
      selfPath: selfPath ?? '$componentId.$field',
      reportError: reportError,
    );

Future<void> _pumpDirectControlled(
  WidgetTester tester,
  ValueNotifier<_DirectConfiguration> configuration, {
  void Function(ValueChanged<Object?> writer)? captureWriter,
}) async {
  await tester.pumpWidget(
    MaterialApp(
      home: ValueListenableBuilder<_DirectConfiguration>(
        valueListenable: configuration,
        builder: (context, value, _) => _RestageA2uiControlledValue(
          dataContext: value.dataContext,
          source: value.source,
          sourcePresent: value.sourcePresent,
          surfaceId: value.surfaceId,
          catalogId: value.catalogId,
          componentId: value.componentId,
          field: value.field,
          selfPath: value.selfPath,
          reportError: value.reportError ??
              (error, stack) => Error.throwWithStackTrace(
                    error,
                    stack ?? StackTrace.current,
                  ),
          builder: (context, raw, present, kind, write) {
            captureWriter?.call(write);
            return Column(
              children: [
                Text(
                  'direct-value:${present ? raw ?? 'null' : 'absent'}:'
                  '${kind.name}',
                ),
                GestureDetector(
                  key: const ValueKey('direct-write-41'),
                  onTap: () => write(41),
                  child: const Text('direct-write-41'),
                ),
              ],
            );
          },
        ),
      ),
    ),
  );
  await tester.pump();
}

Future<void> _tapAndPump(WidgetTester tester, Key key) async {
  await tester.ensureVisible(find.byKey(key));
  await tester.pump();
  await tester.tap(find.byKey(key));
  await tester.pump();
}

void main() {
  testWidgets(
      'private helper distinguishes numeric list literal runtime types',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final dataContext = DataContext(model, DataPath.root);
    final configuration = ValueNotifier(
      _directConfiguration(dataContext, source: <num>[1]),
    );
    addTearDown(configuration.dispose);

    await _pumpDirectControlled(tester, configuration);
    expect(find.text('direct-value:[1]:literal'), findsOneWidget);
    configuration.value = _directConfiguration(
      dataContext,
      source: <num>[1.0],
    );
    await tester.pump();
    expect(find.text('direct-value:[1.0]:literal'), findsOneWidget);
  });

  testWidgets('retained post-dispose writers are inert for every writer route',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final plainContext = DataContext(model, DataPath.root);
    plainContext.update(DataPath('explicit-target'), 7);
    late ValueChanged<Object?> retainedWriter;
    final configuration = ValueNotifier(
      _directConfiguration(
        plainContext,
        source: const <String, Object?>{'path': 'explicit-target'},
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpDirectControlled(
      tester,
      configuration,
      captureWriter: (writer) => retainedWriter = writer,
    );
    await tester.pumpWidget(const SizedBox.shrink());
    retainedWriter(41);
    expect(plainContext.getValue<Object?>(DataPath('explicit-target')), 7);

    configuration.value = _directConfiguration(
      plainContext,
      selfPath: 'literal-self.value',
    );
    await _pumpDirectControlled(
      tester,
      configuration,
      captureWriter: (writer) => retainedWriter = writer,
    );
    await tester.pumpWidget(const SizedBox.shrink());
    retainedWriter(41);
    expect(
      plainContext.getValue<Object?>(DataPath('literal-self.value')),
      isNull,
    );

    final callController = StreamController<Object?>.broadcast(sync: true);
    addTearDown(callController.close);
    final function = _ControlledIntFunction(callController);
    final callContext = DataContext(
      model,
      DataPath.root,
      functions: <ClientFunction>[function],
    );
    configuration.value = _directConfiguration(
      callContext,
      source: const <String, Object?>{'call': 'currentInt'},
      selfPath: 'call-self.value',
    );
    await _pumpDirectControlled(
      tester,
      configuration,
      captureWriter: (writer) => retainedWriter = writer,
    );
    await tester.pump();
    await tester.pumpWidget(const SizedBox.shrink());
    retainedWriter(41);
    expect(
      callContext.getValue<Object?>(DataPath('call-self.value')),
      isNull,
    );
  });

  testWidgets('componentId alone resets the retained helper state',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final dataContext = DataContext(model, DataPath.root);
    const selfPath = 'stable-self.value';
    final configuration = ValueNotifier(
      _directConfiguration(
        dataContext,
        componentId: 'component-a',
        selfPath: selfPath,
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpDirectControlled(tester, configuration);
    await _tapAndPump(tester, const ValueKey('direct-write-41'));
    expect(find.text('direct-value:41:localOverride'), findsOneWidget);
    configuration.value = _directConfiguration(
      dataContext,
      componentId: 'component-b',
      selfPath: selfPath,
    );
    await tester.pump();
    expect(find.text('direct-value:7:literal'), findsOneWidget);
    expect(dataContext.getValue<Object?>(DataPath(selfPath)), 41);
  });

  testWidgets(
      'obsolete asynchronous cancel failures are swallowed (genui 0.10.1) '
      'without corrupting state',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final cancellation = Completer<void>();
    final events = <String>[];
    final function = _LateAfterCancelClientFunction(
      'failingCancel',
      events,
      cancelFuture: cancellation.future,
    );
    final dataContext = DataContext(
      model,
      DataPath.root,
      functions: <ClientFunction>[function],
    );
    final oldErrors = <Object>[];
    final newErrors = <Object>[];
    final configuration = ValueNotifier(
      _directConfiguration(
        dataContext,
        source: const <String, Object?>{'call': 'failingCancel'},
        reportError: (error, stack) => oldErrors.add(error),
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpDirectControlled(tester, configuration);
    await tester.pump();
    expect(function.executions, hasLength(1));
    configuration.value = _directConfiguration(
      dataContext,
      reportError: (error, stack) => newErrors.add(error),
    );
    await tester.pump();
    expect(events.last, 'cancel:failingCancel:1');
    expect(find.text('direct-value:7:literal'), findsOneWidget);

    // Seed the backing document + snapshot the bound path so a HIDDEN document
    // mutation from the failed obsolete cancel is caught directly — rendered
    // output alone would not reveal a stale write into the data model.
    dataContext.update(DataPath('cancel-integrity-probe'), 'intact');
    final boundValueBeforeCancel = dataContext.getValue<Object?>(
      DataPath('root.value'),
    );

    // Documents upstream genui 0.10.1 behavior: an OBSOLETE (already-rebound-
    // away) binding's failed async cancellation is no longer awaited/consumed by
    // genui (0.9.2 routed its error to the old reportError). The subscription's
    // cancel future is the test's own; ignore its error so it does not surface
    // as an unhandled async error, then assert the failure is NOT reported and
    // state is not corrupted.
    cancellation.future.ignore();
    cancellation.completeError(
      StateError('deterministic cancel failure'),
      StackTrace.current,
    );
    await tester.pump();
    expect(oldErrors, isEmpty);
    expect(newErrors, isEmpty);
    expect(find.text('direct-value:7:literal'), findsOneWidget);
    // Document integrity, read straight off the DataContext: the failed obsolete
    // cancel mutated NOTHING — the seeded probe and the bound path are intact.
    expect(
      dataContext.getValue<Object?>(DataPath('cancel-integrity-probe')),
      'intact',
    );
    expect(
      dataContext.getValue<Object?>(DataPath('root.value')),
      boundValueBeforeCancel,
    );

    configuration.value = _directConfiguration(
      dataContext,
      source: 8,
      reportError: (error, stack) => newErrors.add(error),
    );
    await tester.pump();
    expect(find.text('direct-value:8:literal'), findsOneWidget);
    // ...and the seeded document value still survives the subsequent rebind.
    expect(
      dataContext.getValue<Object?>(DataPath('cancel-integrity-probe')),
      'intact',
    );
  });

  testWidgets(
      'literal refreshes before write then retains value and null overrides',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final dataContext = DataContext(model, DataPath.root);
    final configuration = ValueNotifier(
      _ItemConfiguration(
        type: 'ControlledInt',
        data: const {'value': 7},
        id: 'root',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpConfiguredItem(tester, configuration);
    expect(find.text('controlled-value:7'), findsOneWidget);
    expect(model.getValue<Object?>(DataPath('root.value')), isNull);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {'value': 8},
      id: 'root',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('controlled-value:8'), findsOneWidget);

    await _tapAndPump(tester, const ValueKey('controlled-write-41'));
    expect(find.text('controlled-value:41'), findsOneWidget);
    expect(model.getValue<Object?>(DataPath('root.value')), 41);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {'value': 9},
      id: 'root',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('controlled-value:41'), findsOneWidget);

    await _tapAndPump(tester, const ValueKey('controlled-write-null'));
    expect(find.text('controlled-value:null'), findsOneWidget);
    expect(model.getValue<Object?>(DataPath('root.value')), isNull);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {'value': 10},
      id: 'root',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('controlled-value:null'), findsOneWidget);
  });

  testWidgets('path takes precedence, writes through, and rebinds A to B',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final controller = StreamController<Object?>.broadcast(sync: true);
    addTearDown(controller.close);
    final function = _ControlledIntFunction(controller);
    final dataContext = DataContext(
      model,
      DataPath.root,
      functions: <ClientFunction>[function],
    );
    dataContext
      ..update(DataPath('answer-a'), 9)
      ..update(DataPath('answer-b'), 12);
    final configuration = ValueNotifier(
      _ItemConfiguration(
        type: 'ControlledInt',
        data: const {
          'value': {'path': 'answer-a', 'call': 'currentInt'},
        },
        id: 'root',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpConfiguredItem(tester, configuration);
    expect(find.text('controlled-value:9'), findsOneWidget);
    expect(function.executeCount, 0);

    dataContext.update(DataPath('answer-a'), 10);
    await tester.pump();
    await tester.pump();
    expect(find.text('controlled-value:10'), findsOneWidget);

    await _tapAndPump(tester, const ValueKey('controlled-write-41'));
    expect(dataContext.getValue<Object?>(DataPath('answer-a')), 41);
    expect(find.text('controlled-value:41'), findsOneWidget);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {
        'value': {'path': 'answer-b'},
      },
      id: 'root',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('controlled-value:12'), findsOneWidget);

    dataContext.update(DataPath('answer-b'), 13);
    await tester.pump();
    await tester.pump();
    expect(find.text('controlled-value:13'), findsOneWidget);
  });

  testWidgets(
      'call identity, immediate cancellation, epoch, and disposal are exact',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final events = <String>[];
    final firstFunction = _LateAfterCancelClientFunction('lateInt', events);
    final secondFunction =
        _LateAfterCancelClientFunction('lateIntTwo', events);
    final dataContext = DataContext(
      model,
      DataPath.root,
      functions: <ClientFunction>[firstFunction, secondFunction],
    );
    final firstCall = <String, Object?>{
      'call': 'lateInt',
      'args': <String, Object?>{
        'numbers': <Object?>[1, 2],
        'nested': <String, Object?>{'x': 1, 'y': 2},
      },
    };
    final equalDistinctCall = <String, Object?>{
      'args': <String, Object?>{
        'nested': <String, Object?>{'y': 2.0, 'x': 1.0},
        'numbers': <Object?>[1.0, 2.0],
      },
      'call': 'lateInt',
    };
    final changedListCall = <String, Object?>{
      'call': 'lateInt',
      'args': <String, Object?>{
        'numbers': <Object?>[2, 1],
        'nested': <String, Object?>{'x': 1, 'y': 2},
      },
    };
    final configuration = ValueNotifier(
      _ItemConfiguration(
        type: 'ControlledInt',
        data: {'value': firstCall},
        id: 'root',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpConfiguredItem(tester, configuration);
    await tester.pump();
    expect(firstFunction.executions, hasLength(1));
    firstFunction.executions.single.emitLate(13);
    await tester.pump();
    expect(find.text('controlled-value:13'), findsOneWidget);
    firstFunction.executions.single.emitLate(14);
    await tester.pump();
    expect(find.text('controlled-value:14'), findsOneWidget);

    await tester.tap(find.byKey(const ValueKey('controlled-write-41')));
    expect(firstFunction.executions.single.cancelCount, 1);
    expect(events.last, 'cancel:lateInt:1');
    await tester.pump();
    expect(find.text('controlled-value:41'), findsOneWidget);
    firstFunction.executions.single.emitLate(99);
    await tester.pump();
    expect(find.text('controlled-value:41'), findsOneWidget);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: {'value': equalDistinctCall},
      id: 'root',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(firstFunction.executions, hasLength(1));
    expect(find.text('controlled-value:41'), findsOneWidget);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: {'value': changedListCall},
      id: 'root',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    await tester.pump();
    expect(firstFunction.executions, hasLength(2));
    firstFunction.executions.first.emitLate(77);
    await tester.pump();
    expect(find.text('controlled-value:null'), findsOneWidget);
    firstFunction.executions.last.emitLate(23);
    await tester.pump();
    expect(find.text('controlled-value:23'), findsOneWidget);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {
        'value': {
          'call': 'lateIntTwo',
          'args': <String, Object?>{},
        },
      },
      id: 'root',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    await tester.pump();
    expect(firstFunction.executions.last.cancelCount, 1);
    expect(secondFunction.executions, hasLength(1));
    expect(
      events.indexOf('cancel:lateInt:2'),
      lessThan(events.indexOf('execute:lateIntTwo:1')),
    );
    firstFunction.executions.last.emitLate(88);
    secondFunction.executions.single.emitLate(31);
    await tester.pump();
    expect(find.text('controlled-value:31'), findsOneWidget);

    await tester.pumpWidget(const SizedBox.shrink());
    expect(secondFunction.executions.single.cancelCount, 1);
  });

  testWidgets(
      'every leaf family preserves literal conversion, defaults, and isolated overrides',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final dataContext = DataContext(model, DataPath.root);
    final configuration = ValueNotifier(
      _ItemConfiguration(
        type: 'ControlledLeafFamilies',
        data: const {
          'enabled': 1,
          'label': 123,
          'choice': 'alpha',
          'strings': <Object?>['a', 2, null],
          'integers': <Object?>[1, 2.9, '3', null],
          'doubles': <Object?>[1, 2.5, '3'],
          'numbers': <Object?>[1, 2.5, '3'],
          'booleans': <Object?>[true, 1, false],
          'maybeNumbers': <Object?>[1, null, 2.5, '3'],
        },
        id: 'families',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpConfiguredItem(tester, configuration);
    expect(find.text('families-enabled:false'), findsOneWidget);
    expect(find.text('families-label:123'), findsOneWidget);
    expect(find.text('families-choice:alpha'), findsOneWidget);
    expect(find.text('families-strings:a'), findsOneWidget);
    expect(find.text('families-integers:1,2'), findsOneWidget);
    expect(find.text('families-doubles:1.0,2.5'), findsOneWidget);
    expect(find.text('families-numbers:1,2.5'), findsOneWidget);
    expect(find.text('families-number-types:int,double'), findsOneWidget);
    expect(find.text('families-booleans:true,false'), findsOneWidget);
    expect(find.text('families-maybe-integers:null'), findsOneWidget);
    expect(find.text('families-maybe-numbers:1,null,2.5,null'), findsOneWidget);
    expect(find.text('families-fallback-integers:7,8'), findsOneWidget);

    await _tapAndPump(tester, const ValueKey('families-write-scalars'));
    expect(find.text('families-enabled:true'), findsOneWidget);
    expect(find.text('families-label:local'), findsOneWidget);
    expect(find.text('families-choice:beta'), findsOneWidget);
    expect(dataContext.getValue<Object?>(DataPath('families.enabled')), true);
    expect(dataContext.getValue<Object?>(DataPath('families.label')), 'local');
    expect(
      dataContext.getValue<Object?>(DataPath('families.choice')),
      'beta',
    );

    configuration.value = _ItemConfiguration(
      type: 'ControlledLeafFamilies',
      data: const {
        'enabled': false,
        'label': 'producer',
        'choice': 'alpha',
        'strings': <String>['next'],
        'integers': <int>[8],
        'doubles': <double>[8.5],
        'numbers': <num>[8, 8.5],
        'booleans': <bool>[false],
        'maybeIntegers': <int>[9],
        'maybeNumbers': <num?>[9, null],
      },
      id: 'families',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('families-enabled:true'), findsOneWidget);
    expect(find.text('families-label:local'), findsOneWidget);
    expect(find.text('families-choice:beta'), findsOneWidget);
    expect(find.text('families-strings:next'), findsOneWidget);
    expect(find.text('families-integers:8'), findsOneWidget);
    expect(find.text('families-maybe-integers:9'), findsOneWidget);

    await _tapAndPump(tester, const ValueKey('families-write-lists'));
    expect(find.text('families-strings:local'), findsOneWidget);
    expect(find.text('families-integers:41'), findsOneWidget);
    expect(find.text('families-doubles:4.5'), findsOneWidget);
    expect(find.text('families-numbers:4,4.5'), findsOneWidget);
    expect(find.text('families-number-types:int,double'), findsOneWidget);
    expect(find.text('families-booleans:false'), findsOneWidget);
    expect(find.text('families-maybe-numbers:4,null,4.5'), findsOneWidget);
    expect(find.text('families-fallback-integers:9'), findsOneWidget);
    expect(find.text('families-maybe-integers:9'), findsOneWidget);

    await _tapAndPump(tester, const ValueKey('families-write-null-list'));
    expect(find.text('families-maybe-integers:null'), findsOneWidget);
    configuration.value = _ItemConfiguration(
      type: 'ControlledLeafFamilies',
      data: const {
        'enabled': false,
        'label': 'late',
        'choice': 'alpha',
        'strings': <String>['late'],
        'integers': <int>[99],
        'doubles': <double>[99],
        'numbers': <num>[99],
        'booleans': <bool>[true],
        'maybeIntegers': <int>[99],
        'maybeNumbers': <num?>[99],
        'fallbackIntegers': <int>[99],
      },
      id: 'families',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('families-maybe-integers:null'), findsOneWidget);
    expect(find.text('families-integers:41'), findsOneWidget);
  });

  testWidgets('bool, string, enum, and integer list paths stay two-way reactive',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final dataContext = DataContext(model, DataPath.root)
      ..update(DataPath('enabled-path'), 1)
      ..update(DataPath('label-path'), 41)
      ..update(DataPath('choice-path'), 'alpha')
      ..update(DataPath('integers-path'), <Object?>[1, 2.8, 'x']);
    final configuration = ValueNotifier(
      _ItemConfiguration(
        type: 'ControlledLeafFamilies',
        data: const {
          'enabled': {'path': 'enabled-path'},
          'label': {'path': 'label-path'},
          'choice': {'path': 'choice-path'},
          'strings': <String>[],
          'integers': {'path': 'integers-path'},
          'doubles': <double>[],
          'numbers': <num>[],
          'booleans': <bool>[],
          'maybeNumbers': <num?>[],
        },
        id: 'families',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpConfiguredItem(tester, configuration);
    expect(find.text('families-enabled:true'), findsOneWidget);
    expect(find.text('families-label:41'), findsOneWidget);
    expect(find.text('families-choice:alpha'), findsOneWidget);
    expect(find.text('families-integers:1,2'), findsOneWidget);

    dataContext
      ..update(DataPath('enabled-path'), 'false')
      ..update(DataPath('label-path'), false)
      ..update(DataPath('choice-path'), 'beta')
      ..update(DataPath('integers-path'), <Object?>[3.9, 4]);
    await tester.pump();
    await tester.pump();
    expect(find.text('families-enabled:false'), findsOneWidget);
    expect(find.text('families-label:false'), findsOneWidget);
    expect(find.text('families-choice:beta'), findsOneWidget);
    expect(find.text('families-integers:3,4'), findsOneWidget);

    await _tapAndPump(tester, const ValueKey('families-write-scalars'));
    await _tapAndPump(tester, const ValueKey('families-write-lists'));
    expect(dataContext.getValue<Object?>(DataPath('enabled-path')), true);
    expect(dataContext.getValue<Object?>(DataPath('label-path')), 'local');
    expect(
      dataContext.getValue<Object?>(DataPath('choice-path')),
      'beta',
    );
    expect(
      dataContext.getValue<Object?>(DataPath('integers-path')),
      const <int>[41],
    );

    dataContext
      ..update(DataPath('enabled-path'), 0)
      ..update(DataPath('label-path'), 99)
      ..update(DataPath('choice-path'), 'alpha')
      ..update(DataPath('integers-path'), <Object?>[5.8]);
    await tester.pump();
    await tester.pump();
    expect(find.text('families-enabled:false'), findsOneWidget);
    expect(find.text('families-label:99'), findsOneWidget);
    expect(find.text('families-choice:alpha'), findsOneWidget);
    expect(find.text('families-integers:5'), findsOneWidget);
  });

  testWidgets(
      'call conversions stay reactive until write and list cancellation rejects late data',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final enabledController = StreamController<Object?>.broadcast(sync: true);
    final labelController = StreamController<Object?>.broadcast(sync: true);
    final choiceController = StreamController<Object?>.broadcast(sync: true);
    addTearDown(enabledController.close);
    addTearDown(labelController.close);
    addTearDown(choiceController.close);
    final events = <String>[];
    final integerFunction = _LateAfterCancelClientFunction(
      'currentIntegers',
      events,
    );
    final dataContext = DataContext(
      model,
      DataPath.root,
      functions: <ClientFunction>[
        _ControlledValueFunction('currentEnabled', enabledController),
        _ControlledValueFunction('currentLabel', labelController),
        _ControlledValueFunction('currentChoice', choiceController),
        integerFunction,
      ],
    );
    final configuration = ValueNotifier(
      _ItemConfiguration(
        type: 'ControlledLeafFamilies',
        data: const {
          'enabled': {'call': 'currentEnabled'},
          'label': {'call': 'currentLabel'},
          'choice': {'call': 'currentChoice'},
          'strings': <String>[],
          'integers': {'call': 'currentIntegers'},
          'doubles': <double>[],
          'numbers': <num>[],
          'booleans': <bool>[],
          'maybeNumbers': <num?>[],
        },
        id: 'families',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    );
    addTearDown(configuration.dispose);

    await _pumpConfiguredItem(tester, configuration);
    await tester.pump();
    expect(integerFunction.executions, hasLength(1));
    enabledController.add(0);
    labelController.add(12);
    choiceController.add('beta');
    integerFunction.executions.single.emitLate(<Object?>[1, 2.8, 'x']);
    await tester.pump();
    expect(find.text('families-enabled:true'), findsOneWidget);
    expect(find.text('families-label:12'), findsOneWidget);
    expect(find.text('families-choice:beta'), findsOneWidget);
    expect(find.text('families-integers:1,2'), findsOneWidget);

    await tester.ensureVisible(
      find.byKey(const ValueKey('families-write-lists')),
    );
    await tester.pump();
    await tester.tap(find.byKey(const ValueKey('families-write-lists')));
    expect(integerFunction.executions.single.cancelCount, 1);
    expect(events.last, 'cancel:currentIntegers:1');
    await tester.pump();
    expect(find.text('families-integers:41'), findsOneWidget);
    integerFunction.executions.single.emitLate(<int>[99]);
    await tester.pump();
    expect(find.text('families-integers:41'), findsOneWidget);

    await _tapAndPump(tester, const ValueKey('families-write-scalars'));
    enabledController.add(null);
    labelController.add('late');
    choiceController.add('alpha');
    await tester.pump();
    expect(find.text('families-enabled:false'), findsOneWidget);
    expect(find.text('families-label:local'), findsOneWidget);
    expect(find.text('families-choice:beta'), findsOneWidget);
  });

  testWidgets('kind and component identity changes clear an override',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final controller = StreamController<Object?>.broadcast(sync: true);
    addTearDown(controller.close);
    final function = _ControlledIntFunction(controller);
    final dataContext = DataContext(
      model,
      DataPath.root,
      functions: <ClientFunction>[function],
    );
    dataContext.update(DataPath('answer'), 8);
    final configuration = ValueNotifier(
      _ItemConfiguration(
        type: 'ControlledInt',
        data: const {'value': 5},
        id: 'control-a',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    );
    addTearDown(configuration.dispose);
    await _pumpConfiguredItem(tester, configuration);
    await _tapAndPump(tester, const ValueKey('controlled-write-41'));

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {
        'value': {'path': 'answer'},
      },
      id: 'control-a',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('controlled-value:8'), findsOneWidget);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {
        'value': {'call': 'currentInt'},
      },
      id: 'control-a',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    await tester.pump();
    controller.add(19);
    await tester.pump();
    expect(find.text('controlled-value:19'), findsOneWidget);

    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {'value': 6},
      id: 'control-a',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    await _tapAndPump(tester, const ValueKey('controlled-write-41'));
    configuration.value = _ItemConfiguration(
      type: 'ControlledInt',
      data: const {'value': 7},
      id: 'control-b',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('controlled-value:7'), findsOneWidget);
  });

  testWidgets('two fields and two component IDs do not cross-talk',
      (tester) async {
    final model = InMemoryDataModel();
    addTearDown(model.dispose);
    final dataContext = DataContext(model, DataPath.root);
    final pair = ValueNotifier(
      _ItemConfiguration(
        type: 'ControlledIntPair',
        data: const {'first': 1, 'second': 2},
        id: 'pair',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    );
    addTearDown(pair.dispose);
    await _pumpConfiguredItem(tester, pair);
    await _tapAndPump(tester, const ValueKey('pair-write-first'));
    expect(find.text('pair-first:11'), findsOneWidget);
    expect(find.text('pair-second:2'), findsOneWidget);
    pair.value = _ItemConfiguration(
      type: 'ControlledIntPair',
      data: const {'first': 3, 'second': 4},
      id: 'pair',
      surfaceId: 'surface-a',
      dataContext: dataContext,
    );
    await tester.pump();
    expect(find.text('pair-first:11'), findsOneWidget);
    expect(find.text('pair-second:4'), findsOneWidget);
    await _tapAndPump(tester, const ValueKey('pair-write-second'));
    expect(find.text('pair-first:11'), findsOneWidget);
    expect(find.text('pair-second:22'), findsOneWidget);

    final components = ValueNotifier(<_ItemConfiguration>[
      _ItemConfiguration(
        type: 'ControlledInt',
        data: const {'value': 1},
        id: 'control-a',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
      _ItemConfiguration(
        type: 'ControlledInt',
        data: const {'value': 2},
        id: 'control-b',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    ]);
    addTearDown(components.dispose);
    await _pumpTwoConfiguredItems(tester, components);
    await tester.tap(
      find.byKey(const ValueKey('controlled-write-41')).first,
    );
    await tester.pump();
    expect(find.text('controlled-value:41'), findsOneWidget);
    expect(find.text('controlled-value:2'), findsOneWidget);
    components.value = <_ItemConfiguration>[
      _ItemConfiguration(
        type: 'ControlledInt',
        data: const {'value': 9},
        id: 'control-a',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
      _ItemConfiguration(
        type: 'ControlledInt',
        data: const {'value': 3},
        id: 'control-b',
        surfaceId: 'surface-a',
        dataContext: dataContext,
      ),
    ];
    await tester.pump();
    expect(find.text('controlled-value:41'), findsOneWidget);
    expect(find.text('controlled-value:3'), findsOneWidget);
  });

  testWidgets('semantic context axes and field identity reset independently',
      (tester) async {
    expect(_alternateExactCatalogId, isNot(restageA2uiCatalogId));
    final firstModel = InMemoryDataModel();
    final secondModel = InMemoryDataModel();
    addTearDown(firstModel.dispose);
    addTearDown(secondModel.dispose);
    final firstContext = DataContext(firstModel, DataPath.root);
    final configuration = ValueNotifier(
      _directConfiguration(firstContext),
    );
    addTearDown(configuration.dispose);
    await _pumpDirectControlled(tester, configuration);
    await _tapAndPump(tester, const ValueKey('direct-write-41'));
    expect(find.text('direct-value:41:localOverride'), findsOneWidget);

    final equivalentContext = DataContext(firstModel, DataPath.root);
    configuration.value = _directConfiguration(equivalentContext);
    await tester.pump();
    expect(find.text('direct-value:41:localOverride'), findsOneWidget);

    configuration.value = _directConfiguration(
      equivalentContext,
      surfaceId: 'surface-b',
    );
    await tester.pump();
    expect(find.text('direct-value:7:literal'), findsOneWidget);
    await _tapAndPump(tester, const ValueKey('direct-write-41'));

    configuration.value = _directConfiguration(
      equivalentContext,
      surfaceId: 'surface-b',
      catalogId: _alternateExactCatalogId,
    );
    await tester.pump();
    expect(find.text('direct-value:7:literal'), findsOneWidget);
    await _tapAndPump(tester, const ValueKey('direct-write-41'));

    final secondContext = DataContext(secondModel, DataPath.root);
    configuration.value = _directConfiguration(
      secondContext,
      surfaceId: 'surface-b',
      catalogId: _alternateExactCatalogId,
    );
    await tester.pump();
    expect(find.text('direct-value:7:literal'), findsOneWidget);
    await _tapAndPump(tester, const ValueKey('direct-write-41'));

    final nestedContext = DataContext(secondModel, DataPath('nested'));
    configuration.value = _directConfiguration(
      nestedContext,
      surfaceId: 'surface-b',
      catalogId: _alternateExactCatalogId,
    );
    await tester.pump();
    expect(find.text('direct-value:7:literal'), findsOneWidget);
    await _tapAndPump(tester, const ValueKey('direct-write-41'));

    configuration.value = _directConfiguration(
      nestedContext,
      surfaceId: 'surface-b',
      catalogId: _alternateExactCatalogId,
      field: 'otherValue',
    );
    await tester.pump();
    expect(find.text('direct-value:7:literal'), findsOneWidget);
  });

  testWidgets(
      'real SurfaceController validates and renders a controlled enum path reference',
      (tester) async {
    const surfaceId = 'enum-path-surface';
    final controller = SurfaceController(catalogs: [buildRestageCatalog()]);
    final submissions = <ChatMessage>[];
    final submissionSubscription = controller.onSubmit.listen(submissions.add);
    addTearDown(submissionSubscription.cancel);
    addTearDown(controller.dispose);
    controller
      ..handleMessage(
        CreateSurfaceMessage(
          surfaceId: surfaceId,
          catalogId: restageA2uiCatalogId,
        ),
      )
      ..handleMessage(
        UpdateDataModelMessage(
          surfaceId: surfaceId,
          path: 'choice-path',
          value: 'alpha',
        ),
      )
      ..handleMessage(
        UpdateComponentsMessage(
          surfaceId: surfaceId,
          components: [
            _controlledFamiliesComponent(
              const <String, Object?>{'path': 'choice-path'},
            ),
          ],
        ),
      );
    await tester.pumpWidget(
      MaterialApp(
        home: Surface(surfaceContext: controller.contextFor(surfaceId)),
      ),
    );
    await tester.pump();
    expect(
      submissions,
      isEmpty,
      reason: 'the real GenUI validator must accept the enum path reference',
    );
    expect(find.text('families-choice:alpha'), findsOneWidget);

    controller.handleMessage(
      UpdateDataModelMessage(
        surfaceId: surfaceId,
        path: 'choice-path',
        value: 'beta',
      ),
    );
    await tester.pump();
    await tester.pump();
    expect(find.text('families-choice:beta'), findsOneWidget);
    expect(submissions, isEmpty);
  });

  testWidgets(
      'real SurfaceController validates and renders a controlled enum call reference',
      (tester) async {
    const surfaceId = 'enum-call-surface';
    final choiceController = StreamController<Object?>.broadcast(sync: true);
    addTearDown(choiceController.close);
    final catalog = buildRestageCatalog().copyWith(
      newFunctions: [
        _ControlledValueFunction('currentChoice', choiceController),
      ],
    );
    final controller = SurfaceController(catalogs: [catalog]);
    final submissions = <ChatMessage>[];
    final submissionSubscription = controller.onSubmit.listen(submissions.add);
    addTearDown(submissionSubscription.cancel);
    addTearDown(controller.dispose);
    controller
      ..handleMessage(
        CreateSurfaceMessage(
          surfaceId: surfaceId,
          catalogId: restageA2uiCatalogId,
        ),
      )
      ..handleMessage(
        UpdateComponentsMessage(
          surfaceId: surfaceId,
          components: [
            _controlledFamiliesComponent(
              const <String, Object?>{'call': 'currentChoice'},
            ),
          ],
        ),
      );
    await tester.pumpWidget(
      MaterialApp(
        home: Surface(surfaceContext: controller.contextFor(surfaceId)),
      ),
    );
    await tester.pump();
    expect(
      submissions,
      isEmpty,
      reason: 'the real GenUI validator must accept the enum call reference',
    );

    choiceController.add('beta');
    await tester.pump();
    expect(find.text('families-choice:beta'), findsOneWidget);
    choiceController.add('alpha');
    await tester.pump();
    expect(find.text('families-choice:alpha'), findsOneWidget);
    expect(submissions, isEmpty);
  });

  testWidgets('real Surface rebuild retains same ID and resets changed ID',
      (tester) async {
    const surfaceId = 'full-surface';
    final controller = SurfaceController(catalogs: [buildRestageCatalog()]);
    addTearDown(controller.dispose);
    controller
      ..handleMessage(
        CreateSurfaceMessage(
          surfaceId: surfaceId,
          catalogId: restageA2uiCatalogId,
        ),
      )
      ..handleMessage(
        UpdateComponentsMessage(
          surfaceId: surfaceId,
          components: const [
            {
              'id': 'root',
              'component': 'ControlledScalarHost',
              'child': 'control-a',
            },
            {'id': 'control-a', 'component': 'ControlledInt', 'value': 17},
          ],
        ),
      );
    await tester.pumpWidget(
      MaterialApp(
        home: Surface(surfaceContext: controller.contextFor(surfaceId)),
      ),
    );
    await tester.pump();
    expect(find.text('controlled-value:17'), findsOneWidget);
    await _tapAndPump(tester, const ValueKey('controlled-write-41'));
    expect(find.text('controlled-value:41'), findsOneWidget);

    controller.handleMessage(
      UpdateComponentsMessage(
        surfaceId: surfaceId,
        components: const [
          {
            'id': 'root',
            'component': 'ControlledScalarHost',
            'child': 'control-a',
          },
          {'id': 'control-a', 'component': 'ControlledInt', 'value': 99},
        ],
      ),
    );
    await tester.pump();
    expect(find.text('controlled-value:41'), findsOneWidget);

    controller.handleMessage(
      UpdateComponentsMessage(
        surfaceId: surfaceId,
        components: const [
          {
            'id': 'root',
            'component': 'ControlledScalarHost',
            'child': 'control-b',
          },
          {'id': 'control-b', 'component': 'ControlledInt', 'value': 23},
        ],
      ),
    );
    await tester.pump();
    expect(find.text('controlled-value:23'), findsOneWidget);
  });
}
