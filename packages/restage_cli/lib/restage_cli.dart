/// The Restage command-line interface.
///
/// Library entry for consumers that embed the CLI runner — most users will
/// invoke the `restage` binary directly rather than depend on this package.
library;

export 'src/cli.dart';
