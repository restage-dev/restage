import restage.core;
import restage.material;
import restage.cupertino;

widget Paywall = Scaffold(backgroundColor: 0xFF101010, body: Column(children: [Text(text: "Go Premium"), FilledButton(onPressed: event "go" {}, child: Text(text: "Continue"))]));
