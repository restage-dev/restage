# Changelog

## 0.1.0

- Add the initial pre-release Flutter harness that connects customer widget
  registrations to an isolated RFW preview host.
- Add the bounded browser parent-window transport and per-blob render-bundle
  entry selection.
