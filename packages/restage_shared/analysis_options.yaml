include: ../../analysis_options.yaml

# rfw_formats/ is vendored verbatim from rfw 1.1.3 (BSD-3, Flutter team).
# We don't apply our lints to it — modifications would defeat the purpose
# of vendoring (byte-equivalent maintainability with upstream).
analyzer:
  exclude:
    - 'lib/src/rfw_formats/**'
